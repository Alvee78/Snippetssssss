{"a":{
	"prefix": "Circlesleftrightturn",
	"body": [
	  "//To find present position after a move in a circle 1-N ...",
	  "int circlesRightLeftTern(int number,int direction,int now,int moves) {",
	  "    if(direction == 1){",
	  "        return (now+moves>number)?(now+moves)%number:now+moves;",
	  "    }else{",
	  "        return now-moves<1?number+(now-moves)%number:now-moves;",
	  "    }",
	  "}",
	  ""
	],
	"description": "a"
  }
}
